name: Manual Build & Test (SPM)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build with Swift Package Manager
        run: swift build --configuration debug

      - name: Run tests
        run: swift test
